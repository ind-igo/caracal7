# Decisions

Implementation decisions that the spec does not settle. Dated, newest last.

- 2026-09-04: pixi with the stable Mojo channel (Mojo 1.0.0). Tests run with `mojo run -I src`, one `TestSuite` runner per file, since `mojo test` no longer exists.
